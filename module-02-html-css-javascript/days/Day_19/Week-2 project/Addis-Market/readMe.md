# preventDefault() → stops the form from refreshing the page.
# createElement() → creates every shopping-list element dynamically.
# append() → adds the elements to the DOM.
# Event delegation → only one click listener is attached to shoppingList, rather than creating separate listeners for every item.
# classList.toggle("bought") → changes an item's bought state.
# dataset.price → stores the item's price on its row.
# total += itemPrice → adds a new item's price.
# total -= itemPrice → subtracts the price when an item is deleted.
# toFixed(2) → displays the total as two decimal places.